export class Persona {
  peso?: number;
  altura?: number;

  constructor(peso?: number, altura?: number) {
    this.peso = peso;
    this.altura = altura;
  }
}

/*?
 * Se pone el signo de interrogación '?' para no tener que darle un valor por defecto
 */
